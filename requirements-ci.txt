# TMC Chatbot — CI/CD Test Dependencies
# Minimal install for fast GitHub Actions runs (< 30s install time)
# Full ML stack (torch, chromadb, sentence-transformers) lives in requirements.txt
# Python: 3.11 (ubuntu-latest runner)

# Core web framework — needed for import checks
Flask==3.1.3
Flask-CORS==6.0.2
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.4.1

# Session & security (app.py imports these at module level)
Flask-Session==0.8.0
Flask-Limiter==4.1.1
Flask-WTF==1.3.0
bcrypt==5.0.0

# HTTP
requests==2.34.2
urllib3==2.7.0

# Config / serialization
python-dotenv==1.2.2
PyYAML==6.0.3
pydantic==2.13.4

# Testing framework
pytest==7.4.3
pytest-cov==4.1.0
